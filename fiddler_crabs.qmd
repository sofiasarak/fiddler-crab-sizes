---
title: "Day 10 Github Practice"
format: html
editor: visual
execute: 
  echo: false
---

```{r}
#install.packages("lterdatasampler")
library(tidyverse)
library(lterdatasampler)
library(kableExtra)
crabs <- pie_crab
```

Creating carapace plot
```{r}
#| fig-cap: "this is my caption"
ggplot(data = crabs, aes(x = size, y = latitude))+
  geom_point()
```

```{r}
crabs_table <- crabs %>% 
  group_by(site) %>% 
  summarize(mean = round(mean(size), 2), sd = sd(size), sample_size = n())

crabs_table
```

```{r}
crabs_table |> 
  kable(col.names = c("Site",
                      "Mean (mm)",
                      "Standard deviation (mm)",
                      "Sample size")) |> 
  kable_classic()
```

